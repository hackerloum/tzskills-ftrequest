PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=feature_tracker

# Cloud MySQL (Render, PlanetScale, Railway, etc.) often needs SSL:
# DB_SSL=true
# If the provider uses a self-signed cert, try:
# DB_SSL=true
# DB_SSL_REJECT_UNAUTHORIZED=false
